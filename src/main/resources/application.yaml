server:
  port: 9001

spring:
  application:
    name: ms-grupos

  cloud:
    compatibility-verifier:
      enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: adelabot.noresponder@gmail.com
    password: cayf bzhb dgtb nogh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:mysql://localhost:3306/group_service_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# JWT Configuration - MISMO SECRET QUE MS-AUTH
jwt:
  secret: a6053a18613b29a2aa3407cccfe6fd47c681ed3cc7808e46392c2f0b1b2259a90e1a3f426572715f94c4ccc61aa108e3b94bb0358dbf462284091ac81cf1b94f

# CORS Configuration
cors:
  allowed-origins: http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# Swagger Documentation
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /docs/swagger-ui.html

# Logging para debug
logging:
  level:
    com.example.chaea: DEBUG
    com.example.chaea.security: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
